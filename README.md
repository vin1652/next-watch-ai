# next-watch-ai

A CLI tool that recommends what to watch next using a film-student style multi-agent workflow.

## Setup
1) Create `.env`:
- FIRECRAWL_API_KEY=...
- GROQ_API_KEY=...

2) Install:
pip install -r requirements.txt

## Run
python -m next_watch_ai.cli
